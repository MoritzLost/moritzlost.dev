<article class="article">
    <slot />
</article>

<style lang="postcss">
    article {
        > :global(* + *) {
            margin-top: var(--stack-gap);
        }
        > :global(* + h1),
        > :global(* + h2),
        > :global(* + h3),
        > :global(* + h4),
        > :global(* + h5),
        > :global(* + h6) {
            --stack-gap: var(--stack-gap-headings);
        }
    }
</style>
